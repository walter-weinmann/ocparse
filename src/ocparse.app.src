{application, ocparse,
    [
        {applications,
            [
                kernel,
                stdlib
            ]
        },
        {description, "LALR grammar based Cypher parser using the grammar rules from the openCypher project"},
        {mod, {ocparse, []}},
        {modules,
            [
                ocparse,
                ocparse_legacy
            ]
        },
        {vsn, "1.2.6"}
    ]
}.
