{application, ocparse,
    [{description, "A LALR open cypher parser and compiler"},
        {vsn, "1.0.0"},
        {registered, []},
        {applications, [kernel, stdlib]},
        {mod, {ocparse, []}},
        {env, []},
        {modules, [oclexer, ocparse, ocparse_fold, octest,
            oclexer_legacy, ocparse_legacy, ocparse_fold_legacy, octest_legacy]}]}.
